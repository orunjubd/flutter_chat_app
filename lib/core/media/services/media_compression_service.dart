import '../models/media_draft.dart';

class MediaCompressionService {
  const MediaCompressionService();

  Future<MediaDraft> compress(MediaDraft draft) async {
    // Phase 4.2
    // flutter_image_compress will be integrated here.

    return draft;
  }
}
